---
# location for cached repositories
:cachedir: '/var/cache/r10k'

# git repositories containing environments
:sources:
  :hiera:
    :remote:  git://repo.apac.inin.com/hiera-data.git
    :basedir: '/var/lib/hiera'

# purge non-existing data
:purgedirs:
  - '/var/lib/hiera'
